{
  "private": true,
  "name": "remnote-format-color",
  "title": "Format Color",
  "description": "Automatically colors formatted text (Chinese, Italic, Bold, Underline) with custom text and highlight colors.",
  "version": "0.1.1",
  "license": "MIT",
  "scripts": {
    "check-types": "tsc",
    "dev": "cross-env NODE_ENV=development webpack serve --color --progress --no-open",
    "build": "npx remnote-plugin validate && shx rm -rf dist && cross-env NODE_ENV=production webpack --color --progress && cd dist && bestzip ../PluginZip.zip ./*"
  },
  "dependencies": {
    "@remnote/plugin-sdk": "latest",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.2",
    "@types/react-dom": "^17.0.2",
    "bestzip": "^2.2.1",
    "copy-webpack-plugin": "^14.0.0",
    "cross-env": "^7.0.3",
    "esbuild-loader": "^4.5.0",
    "html-webpack-plugin": "^5.5.0",
    "shx": "^0.3.4",
    "typescript": "^5.9.3",
    "webpack": "^5.73.0",
    "webpack-cli": "^4.10.0",
    "webpack-dev-server": "^6.0.0"
  }
}
